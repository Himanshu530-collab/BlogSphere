<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('./partials/head') %>
    <title>Add New Blog</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <%- include('./partials/nav') %>

    <div class="container mt-5">
        <div class="card shadow-sm">
            <div class="card-header text-center font-weight-bold">
                Add a New Blog
            </div>
            <div class="card-body">
                <% if (errorMessage) { %>
                    <div class="alert alert-danger">
                        <%= errorMessage %> <!-- Display error message here -->
                    </div>
                <% } %>

                <form action="/blog/add-new" method="POST" enctype="multipart/form-data" onsubmit="return confirmAdd()">
                    <div class="form-group">
                        <label for="title">Title</label>
                        <input type="text" class="form-control" id="title" name="title" value="<%= title %>" required>
                    </div>
                    <div class="form-group">
                        <label for="body">Body</label>
                        <textarea class="form-control" id="body" name="body" rows="4" required><%= body %></textarea>
                    </div>
                    <div class="form-group">
                        <label for="coverImage">Cover Image (optional)</label>
                        <input type="file" class="form-control-file" id="coverImage" name="coverImage" accept="image/*">
                    </div>
                    <button type="submit" class="btn btn-success btn-block">Add Blog</button>
                </form>
            </div>
        </div>
    </div>

    <script>
        function confirmAdd() {
            return confirm("Are you sure you want to add this blog?");
        }
    </script>

    <%- include('./partials/scripts') %>
</body>
</html>
