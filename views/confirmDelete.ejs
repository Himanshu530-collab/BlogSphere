<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('./partials/head') %>
    <title>Delete Blog</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <%- include('./partials/nav') %>

    <div class="container mt-5">
        <div class="card shadow-sm">
            <div class="card-header text-center text-danger font-weight-bold">
                Are you sure you want to delete the blog: <%= blog.title %>?
            </div>
            <div class="card-body">
                <form action="/blog/delete/<%= blog._id %>" method="POST" onsubmit="return confirmDelete()">
                    <div class="alert alert-warning">
                        <strong>Warning!</strong> This action cannot be undone.
                    </div>
                    <div class="d-flex justify-content-between">
                        <button type="submit" class="btn btn-danger">Yes, delete</button>
                        <a href="/blog/<%= blog._id %>" class="btn btn-secondary">No, go back</a>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <script>
        function confirmDelete() {
            return confirm("Are you sure you want to delete this blog?");
        }
    </script>

    <%- include('./partials/scripts') %>
</body>
</html>
